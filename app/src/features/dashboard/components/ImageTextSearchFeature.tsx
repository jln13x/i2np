import { ComingSoon } from './ComingSoon';

export const ImageTextSearchFeature = () => {
  return (
    <ComingSoon
      title="Image Text Search"
      description="Search through your images on Notion to find text in them"
    />
  );
};
