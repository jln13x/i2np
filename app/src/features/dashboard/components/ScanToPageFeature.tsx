import { ComingSoon } from './ComingSoon';

export const ScanToPageFeature = () => {
  return (
    <ComingSoon
      title="Scan to Page"
      description="Scan your documents and upload them as PDF to Notion"
    />
  );
};
